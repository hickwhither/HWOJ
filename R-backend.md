# HWOJ-FastAPI

## Installation

Make sure `uv` is installed

Install / sync
```sh
uv sync
```

## local_setting.py
copy `setings.py`

## Usage
production
```sh
uv run fastapi run
```

dev
```sh
uv run fastapi dev main.py
```